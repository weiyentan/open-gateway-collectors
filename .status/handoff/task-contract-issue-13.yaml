# .status/handoff/task-contract-issue-13.yaml
# Generated by task-router for issue #13
# Routing rationale: ambiguous/design-bearing work (signal #7) — documentation task requires executor to make design decisions about document structure, diagram format (Mermaid/ASCII/image), content depth, and file placement; no existing documentation pattern exists for workflow docs in the repo (only ADRs and README exist).

tier: T3
title: "Create a new issue - Documentation workflow on how the collectors works"
issue_number: 13
goal: "Write a documentation file explaining how the collector works in its operational workflow, supplemented by a diagram that references and synthesizes content from the project's ADRs."

acceptance_criteria:
  - "A new documentation file exists under `docs/` explaining the collector's operational workflow (startup → database discovery → identity management → incremental cursor-based reading → usage ingestion → heartbeat → retry backoff)"
  - "The documentation references and supplements content from all five ADRs (ADR-0001 through ADR-0005)"
  - "A diagram (Mermaid, ASCII art, or image) is embedded in the documentation to illustrate the collector's lifecycle and data flow"
  - "The documentation uses consistent domain language from CONTEXT.md (Collector, Source Database, Usage Record, Ingest Batch, Heartbeat, Cursor, Idempotency Key, etc.)"
  - "The documentation accurately reflects the current source code structure in `internal/` packages (collector, config, gateway, heartbeat, identity, sqlite, state)"
  - "The documentation includes a brief 'Lifecycle' or 'Flow' section that walks through a typical push cycle end-to-end"

allowed_paths:
  - "docs/*.md"

forbidden_changes:
  - "Do not modify any source code files under cmd/, internal/, or testdata/"
  - "Do not modify existing ADR files under docs/adr/"
  - "Do not modify CI/CD, build, or configuration files (.goreleaser.yaml, Makefile, Dockerfile, .github/)"
  - "Do not modify CONTEXT.md or .opencode-workflow.yaml"

expected_change_types:
  - add

validation_requirements:
  - "Documentation is consistent with domain language defined in CONTEXT.md — verify no term mismatch"
  - "All ADR references use correct file paths and accurate titles"
  - "The diagram renders correctly (valid Mermaid syntax if used, or legible ASCII art)"
  - "go build ./... passes (to confirm no source code files were accidentally modified)"
  - "go vet ./... passes"

escalation_path: route_to_human

stop_conditions:
  - "More than 4 files are modified"
  - "Any .go source code file is modified, created, or deleted"
  - "Any existing ADR document under docs/adr/ is modified or deleted"
  - "A previously-passing test now fails"

review_mode: mandatory
