# .status/handoff/task-contract-issue-3.yaml
# Generated by task-router for issue #3

tier: T2
goal: "Implement the SQLite usage reader (OpenCodeReader) that queries assistant message usage data from OpenCode databases and maps it into canonical UsageRecord types."
acceptance_criteria:
  - "ReadRecords() returns correctly-mapped UsageRecord values from a test fixture database"
  - "User messages without tokens.input in message.data JSON are skipped"
  - "Records are ordered by message.time_updated ascending"
  - "Cursor filtering returns only records with time_updated > cursor timestamp"
  - "Batch limit parameter is respected (default 500)"
  - "PRAGMA query_only = 1 is set on the read-only connection"
  - "Zero-cost records (local/free providers) are still included as usage records"
  - "go vet ./... passes and all internal/sqlite tests pass"
allowed_paths:
  - "internal/sqlite/"
  - "testdata/"
forbidden_changes:
  - "Do not modify files outside internal/sqlite/ or testdata/"
  - "Do not include message.data fields beyond the UsageRecord mapping (privacy constraint)"
expected_change_types:
  - add
validation_requirements:
  - "Run go vet ./... — must pass without warnings"
  - "Run go test ./internal/sqlite/... — all tests must pass"
  - "Run go build ./... — must compile without errors"
escalation_path: ask_user
stop_conditions:
  - "More than 3 files are modified"
  - "A previously-passing test now fails"
  - "A file outside internal/sqlite/ or testdata/ is modified"
  - "The SQL query deviates from the issue-specified JOIN and WHERE pattern without justification"
review_mode: auto
