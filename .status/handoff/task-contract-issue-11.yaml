# .status/handoff/task-contract-issue-11.yaml
# Generated by task-router for issue #11

issue_number: 11
title: "Add darwin and windows build targets to goreleaser"
summary: "Expand the goreleaser build matrix to add darwin/amd64, darwin/arm64, and windows/amd64 targets. Keep linux/amd64 as the production target. Set meaningful per-platform archive filenames. No workflow changes needed."
tier: T2
required_skill: none
review_mode: auto
acceptance_criteria:
  - "Build targets include darwin/amd64, darwin/arm64, windows/amd64 in the builds section"
  - "linux/amd64 is preserved as the production target"
  - "Archive filenames are distinguishable per platform (e.g. opencode-collector_{{ .Version }}_{{ .Os }}_{{ .Arch }}.tar.gz)"
  - "All four platform binaries appear in the release artifact list"
  - "Checksums include all platform binaries"
  - "goreleaser check passes with no config errors"
files_to_change:
  - ".goreleaser.yaml"
forbidden_changes:
  - "Do not modify files outside .goreleaser.yaml"
  - "Do not change or remove the linux/amd64 production target"
  - "Do not modify GitHub Actions workflow files"
escalation_path: ask_user
